# Contributing

This is a personal Claude Code harness, not a published plugin or community resource. Edits, trims, and rewrites happen freely as my workloads evolve.

If you're reading this from a fork: feel free to copy any agents/skills/commands/hooks into your own `~/.claude/` setup. Test changes locally with `node tests/run-all.js` before pushing.
